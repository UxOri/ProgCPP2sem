#include <iostream>
#include <cmath>
#include <algorithm>
using std::cin;
using std::cout;
using std::endl;

int main () {
    int N;
    cin >> N;
    
    int* data = new int[N];
    for (int i = 0; i < N; i++)
        cin >> data[i];
    
}       
