#include <iostream>
#include <cmath>
#include <algorithm>
#include <iomanip>
using std::cin;
using std::cout;
using std::endl;

int main () {
    int N;
    cin >> N;

    int* data = new int[N];
    for (int i = 0; i < N; i++)
        cin >> data[i];
    std::sort (data, data + N);
    /*for (int i = 0; i < N; i++)
        cout << data[i];  */
    double S = 0;
    for (int i = 0; i < N; i++)
        for (int j = 0; j < N - i; j++) {
            if (fabs(data[i] - data[i+j]))
        }

    return 0;
}
